# 抽奖网页程序 - 设计文档

## 0. 产品定位

### 0.1 核心功能

本程序是一款灵活的转盘抽奖工具，支持两种主要应用场景：

**场景一：抽奖品**
- 适用于各类活动、促销、年会等场景
- 设置奖品列表进行随机抽奖
- 支持权重控制中奖概率

**场景二：抽人**
- 适用于公司、班级、团队等场景
- 设置人员名单进行随机抽取
- 典型应用：年会抽奖员工、班级随机点名、团队随机分配任务、会议随机选择发言人
- 支持批量导入人员名单（每行一个姓名）
- 支持设置最大中奖次数，避免重复抽中同一人
- 支持权重设置（权重0=不参与抽取，权重100=必定抽中）

### 0.2 功能特性

- 纯前端实现，无需后端服务
- 支持多模式独立配置
- 支持权重概率控制（0-100百分比）
- 支持批量导入名单
- 支持中奖次数限制
- 支持中奖次数可视化显示
- 响应式设计，支持移动端（已优化手机端渲染）

---

## 1. 系统架构

### 1.1 整体架构

```
┌───────────────────────────────────────────────────────────────┐
│                         用户界面层                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐           │
│  │ 主页面  │  │ 设置面板 │  │ 帮助面板 │  │ 结果弹窗 │           │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘           │
└───────┼────────────┼────────────┼────────────┼────────────────┘
        │            │            │            │
┌───────┼────────────┼────────────┼────────────┼────────────────┐
│       │         业务逻辑层         │            │               │
│  ┌────▼────┐  ┌────▼────┐  ┌────▼────┐  ┌────▼────┐            │
│  │转盘引擎  │  │配置管理  │  │抽奖逻辑  │  │存储管理  │            │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘            │
└─────────────────────────────────────────────────────────────────┘
        │
┌───────┼─────────────────────────────────────────────────────────┐
│       │              数据持久层                                   │
│  ┌────▼────────────────────────────────────────────────────┐    │
│  │              本地存储管理器 (StorageManager)              │    │
│  │              使用 localStorage 存储配置和历史              │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术选型

| 技术项 | 选型 | 理由 |
|--------|------|------|
| 框架 | 原生 JavaScript (ES6+) | 简单直接，支持异步 |
| 样式 | CSS3 | 原生支持，无需编译 |
| 动画 | CSS Transform + Canvas | 流畅的转盘动画 |
| 存储 | localStorage | 浏览器原生支持，永久有效 |
| 构建 | 无 | 纯静态文件，所见即所得 |

## 2. 目录结构

```
lottery/
├── doc/                          # 文档目录
│   ├── 需求文档.md
│   ├── 设计文档.md
│   └── 使用说明.md
├── src/                          # 源码目录
│   ├── index.html                # 主页面
│   ├── css/
│   │   ├── style.css             # 主样式
│   │   ├── turntable.css         # 转盘样式
│   │   └── modal.css             # 弹窗样式
│   ├── js/
│   │   ├── app.js                # 主程序入口
│   │   ├── turntable.js          # 转盘引擎
│   │   ├── config.js             # 配置管理
│   │   ├── storage.js            # 存储管理
│   │   └── utils.js              # 工具函数
│   └── user/                     # 用户配置示例
│       └── example.json          # 配置示例文件
└── README.md                     # 项目说明
```

## 3. 核心模块设计

### 3.1 存储管理器 (StorageManager)

**职责**：统一管理配置和历史的存储

**核心方法**：
```javascript
class StorageManager {
  // 配置管理
  saveConfig(mode, config)    // 保存配置
  loadConfig(mode)            // 加载配置
  clearConfig(mode)           // 清除配置
  
  // 历史管理
  saveHistory(mode, history)  // 保存历史
  loadHistory(mode)           // 加载历史
  clearHistory(mode)          // 清除历史
  
  // 当前模式
  setCurrentMode(mode)        // 设置当前模式
  getCurrentMode()            // 获取当前模式
}
```

### 3.2 配置管理器 (ConfigManager)

**职责**：管理抽奖配置

**核心方法**：
```javascript
class ConfigManager {
  // 配置操作
  getConfig()                 // 获取当前配置
  updateConfig(updates)       // 更新配置
  resetConfig()               // 重置配置
  
  // 奖品操作
  addPrize(prize)             // 添加奖品
  updatePrize(index, prize)   // 更新奖品
  removePrize(index)          // 删除奖品
  
  // 模式操作
  switchMode(mode)            // 切换模式
  getModes()                  // 获取所有模式
}
```

### 3.3 转盘引擎 (Turntable)

**职责**：绘制转盘和控制动画

**核心方法**：
```javascript
class Turntable {
  constructor(canvas, options)
  
  // 绘制方法
  draw(prizes)                // 绘制转盘
  resizeCanvas()              // 响应窗口大小变化
  
  // 动画方法
  spin(targetIndex)           // 开始旋转
  stop()                      // 停止旋转
  
  // 事件
  onEnd(callback)             // 旋转结束回调
}
```

### 3.4 主程序 (App)

**职责**：协调各模块，处理用户交互

**核心方法**：
```javascript
class App {
  init()                      // 初始化应用
  bindEvents()                // 绑定事件
  handleSpin()                // 处理抽奖
  showResult(prize)           // 显示结果
  
  // 设置面板
  openSettings()              // 打开设置
  closeSettings()             // 关闭设置
  saveSettings()              // 保存设置
}
```

## 4. 数据流设计

### 4.1 初始化流程

```
用户打开页面
    │
    ├─ 解析 URL 参数 (mode)
    │
    ├─ 初始化 StorageManager
    │   └─ 加载 localStorage 中的配置
    │
    ├─ 初始化 ConfigManager
    │   └─ 加载指定模式的配置
    │
    ├─ 初始化 Turntable
    │   └─ 绘制转盘
    │
    └─ 初始化 App
        └─ 绑定事件监听
```

### 4.2 抽奖流程

```
用户点击「开始抽奖」
    │
    ├─ 检查是否正在旋转
    │   └─ 是 → 忽略
    │
    ├─ 根据权重计算目标位置
    │   └─ 权重越大，概率越高
    │
    ├─ 调用 Turntable.spin(targetIndex)
    │   └─ 执行旋转动画
    │
    ├─ 动画结束
    │   ├─ 显示中奖结果
    │   ├─ 更新中奖次数
    │   └─ 保存到历史记录
    │
    └─ 更新 localStorage
```

### 4.3 配置保存流程

```
用户修改配置
    │
    ├─ 验证配置合法性
    │   ├─ 奖品数量 ≥ 2
    │   ├─ 权重范围 0-100
    │   └─ 最大中奖次数 ≥ 0
    │
    ├─ 更新 ConfigManager
    │
    └─ 保存到 localStorage
        └─ key: lottery_config_{mode}
```

## 5. 数据结构设计

### 5.1 配置数据结构

```javascript
{
  "title": "幸运抽奖",           // 抽奖标题
  "prizes": [
    {
      "text": "一等奖",          // 奖品名称
      "icon": "🏆",              // 图标（emoji）
      "color": "#e74c3c",       // 背景色
      "weight": 10,             // 权重（0-100）
      "maxWins": 1,             // 最大中奖次数
      "winCount": 0             // 已中奖次数
    }
  ],
  "mode": "default",            // 模式名称
  "updatedAt": "2026-04-09T00:00:00.000Z"
}
```

### 5.2 历史记录结构

```javascript
[
  {
    "prize": "一等奖",          // 中奖名称
    "icon": "🏆",              // 图标
    "timestamp": "2026-04-09T00:00:00.000Z"
  }
]
```

### 5.3 本地存储 Key

- `lottery_config_{mode}` - 配置数据
- `lottery_history_{mode}` - 抽奖历史

## 6. 权重算法设计

### 6.1 权重转概率

```javascript
// 权重范围：0-100（百分比）
// 权重为 0 表示永不中奖
// 权重为 100 表示必中

function calculateProbability(prizes) {
  // 过滤掉权重为 0 的奖品
  const validPrizes = prizes.filter(p => p.weight > 0);
  
  // 计算总权重
  const totalWeight = validPrizes.reduce((sum, p) => sum + p.weight, 0);
  
  // 计算每个奖品的概率
  return validPrizes.map(p => ({
    ...p,
    probability: p.weight / totalWeight
  }));
}
```

### 6.2 随机选择算法

```javascript
function selectPrize(prizes) {
  // 过滤已达到最大中奖次数的奖品
  const availablePrizes = prizes.filter(
    p => p.winCount < p.maxWins
  );
  
  // 计算概率
  const withProb = calculateProbability(availablePrizes);
  
  // 随机选择
  const random = Math.random();
  let cumulative = 0;
  
  for (const prize of withProb) {
    cumulative += prize.probability;
    if (random <= cumulative) {
      return prize;
    }
  }
  
  return withProb[withProb.length - 1];
}
```

## 7. 界面设计

### 7.1 主界面

```
┌─────────────────────────────────────┐
│              [抽奖标题]               │
│                                     │
│         ┌───────────────┐           │
│         │               │           │
│         │    转盘区域    │           │
│         │               │           │
│         └───────┬───────┘           │
│                 │                   │
│                 ▼                   │
│         [ 开始抽奖 按钮 ]            │
│                                     │
│         ┌───────────────┐           │
│         │   中奖列表     │           │
│         │  一等奖 x1    │           │
│         │  二等奖 x2    │           │
│         └───────────────┘           │
│                                     │
│  [设置] [帮助] [重置]               │
└─────────────────────────────────────┘
```

### 7.2 设置面板

```
┌─────────────────────────────────────┐
│  设置                           [X] │
├─────────────────────────────────────┤
│                                     │
│  抽奖标题: [________________]       │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 奖品列表                      │   │
│  │ ┌───┬───┬───┬───┬───┐      │   │
│  │ │名称│图标│颜色│权重│操作│      │   │
│  │ ├───┼───┼───┼───┼───┤      │   │
│  │ │一等奖│🏆│红色│10 │删除│      │   │
│  │ └───┴───┴───┴───┴───┘      │   │
│  │ [+ 添加奖品]                 │   │
│  └─────────────────────────────┘   │
│                                     │
│  [清空] [导入] [保存]               │
└─────────────────────────────────────┘
```

## 8. 错误处理

| 错误场景 | 处理方式 |
|---------|---------|
| 奖品数量不足 | 提示"请至少添加2个奖品" |
| 权重全为0 | 提示"请设置至少一个权重大于0的奖品" |
| localStorage 不可用 | 使用内存存储，提示"本地存储不可用" |
| Canvas 不支持 | 提示"您的浏览器不支持 Canvas" |

## 9. 性能优化

### 9.1 渲染优化

- Canvas 绑定 DPI 缩放，避免模糊
- 使用 requestAnimationFrame 进行动画
- 窗口 resize 时防抖处理

### 9.2 存储优化

- 配置变更时防抖保存
- 历史记录限制最大条数（100条）

---

**版本**: v1.0.0  
**更新日期**: 2026-04-09  
**作者**: 柳飘飘