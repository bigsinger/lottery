# 抽奖网页程序 - 设计文档

## 0. 产品定位

### 0.1 核心功能

本程序是一款灵活的转盘抽奖工具，支持两种主要应用场景：

**场景一：抽奖品**
- 适用于各类活动、促销、年会等场景
- 设置奖品列表进行随机抽奖
- 支持权重控制中奖概率

**场景二：抽人**
- 适用于公司、班级、团队等场景
- 设置人员名单进行随机抽取
- 典型应用：年会抽奖员工、班级随机点名、团队随机分配任务、会议随机选择发言人
- 支持批量导入人员名单（每行一个姓名）
- 支持设置最大中奖次数，避免重复抽中同一人
- 支持权重设置（权重0=不参与抽取，权重100=必定抽中）

### 0.2 功能特性

- 纯前端实现，可选后端服务（多设备同步）
- 支持多模式独立配置
- 支持多设备配置同步（user参数）
- 支持权重概率控制（0-100百分比）
- 支持批量导入名单
- 支持中奖次数限制
- 支持中奖次数可视化显示
- 响应式设计，支持移动端（已优化手机端渲染）

---

## 1. 系统架构

### 1.1 整体架构

```
┌───────────────────────────────────────────────────────────────┐
│                         用户界面层                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐           │
│  │ 主页面  │  │ 设置面板 │  │ 帮助面板 │  │ 结果弹窗 │           │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘           │
└───────┼────────────┼────────────┼────────────┼────────────────┘
        │            │            │            │
┌───────┼────────────┼────────────┼────────────┼────────────────┐
│       │         业务逻辑层         │            │               │
│  ┌────▼────┐  ┌────▼────┐  ┌────▼────┐  ┌────▼────┐            │
│  │转盘引擎  │  │配置管理  │  │抽奖逻辑  │  │存储管理  │            │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘            │
└─────────────────────────────────────────────────────────────────┘
        │
┌───────┼─────────────────────────────────────────────────────────┐
│       │              数据持久层                                   │
│  ┌────▼────────────────────────────────────────────────────┐    │
│  │              统一存储管理器 (StorageManager)              │    │
│  │  ┌──────────────────┐      ┌──────────────────┐         │    │
│  │  │  本地存储        │      │  服务端存储       │         │    │
│  │  │  (localStorage)  │      │  (API + JSON)    │         │    │
│  │  │  - 无 user 参数  │      │  - 有 user 参数  │         │    │
│  │  │  - 单设备使用    │      │  - 多设备同步    │         │    │
│  │  └──────────────────┘      └──────────────────┘         │    │
│  └─────────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────────┘
```

### 1.2 技术选型

| 技术项 | 选型 | 理由 |
|--------|------|------|
| 框架 | 原生 JavaScript (ES6+) | 简单直接，支持异步 |
| 样式 | CSS3 | 原生支持，无需编译 |
| 动画 | CSS Transform + Canvas | 流畅的转盘动画 |
| 存储（本地） | localStorage | 浏览器原生支持，永久有效 |
| 存储（服务端） | PHP/Node.js API | 多设备配置同步 |
| 构建 | 无 | 纯静态文件，所见即所得 |

## 2. 目录结构

```
lottery/
├── doc/                          # 文档目录
│   ├── 需求文档.md
│   ├── 设计文档.md
│   └── 使用说明.md
├── src/                          # 源码目录
│   ├── index.html                # 主页面
│   ├── api/                      # API 目录（多设备同步）
│   │   └── api.js                # Node.js API
│   ├── user/                     # 用户配置目录（多设备同步）
│   │   └── {user}.json           # 用户配置文件
│   ├── css/
│   │   ├── style.css             # 主样式
│   │   ├── turntable.css         # 转盘样式
│   │   └── modal.css             # 弹窗样式
│   ├── js/
│   │   ├── app.js                # 主程序入口
│   │   ├── turntable.js          # 转盘引擎
│   │   ├── config.js             # 配置管理
│   │   ├── storage.js            # 存储管理（本地存储）
│   │   └── utils.js              # 工具函数
│   └── images/                   # 图片资源（预留）
├── temp/                         # 临时目录（开发参考）
└── README.md                     # 项目说明
```

## 3. 模块设计

### 3.1 转盘引擎 (turntable.js)

**职责**：
- Canvas 转盘绘制
- 旋转动画控制
- 响应式尺寸适配

**关键方法**：
```javascript
class Turntable {
  constructor(canvas, options)
  draw(prizes)           // 绘制转盘
  spin(targetIndex, callback)  // 开始旋转
  reset()                // 重置转盘
  resize()               // 响应式调整
}
```

**手机端优化**：
- 动态计算 Canvas 尺寸
- 添加最小尺寸约束
- 改进 DPR 处理

### 3.2 配置管理 (config.js)

**职责**：
- 配置加载/保存
- 默认配置生成
- 模式隔离管理

**关键方法**：
```javascript
class ConfigManager {
  constructor(mode)
  getConfig()            // 获取配置
  saveConfig(config)     // 保存配置
  getDefaultConfig()     // 默认配置
  resetConfig()          // 重置配置
}
```

### 3.3 存储管理 (storage.js)

**职责**：
- 统一存储管理（本地和服务端）
- 根据 user 参数自动选择存储方式
- 配置/历史数据管理
- 错误处理

**关键类和方法**：
```javascript
// 本地存储管理器（单设备）
class LocalStorageManager {
  constructor(mode)
  saveConfig(config)     // 保存配置到 localStorage
  loadConfig()           // 从 localStorage 加载配置
  saveHistory(result)    // 保存抽奖历史
  loadHistory()          // 加载抽奖历史
  clearConfig()          // 清除配置
  clearHistory()         // 清除历史
}

// 统一存储管理器
class StorageManager {
  constructor(user, mode)
  getStorageType()       // 获取存储类型（始终返回 'local'）
  isServerStorage()      // 是否为服务端存储（始终返回 false）
  saveConfig(config)     // 保存配置
  loadConfig()           // 加载配置
  saveHistory(result)    // 保存抽奖历史
  loadHistory()          // 加载抽奖历史
  clearConfig()          // 清除配置
  clearHistory()         // 清除历史
}
```

**存储逻辑**：
```javascript
class StorageManager {
  constructor(user, mode) {
    // 使用 localStorage 进行本地存储
    // user 参数保留用于兼容性，但不再用于服务端存储
    this.configKey = `lottery_config_${mode}`;
    this.historyKey = `lottery_history_${mode}`;
  }
}
```

### 3.4 抽奖逻辑 (app.js)

**职责**：
- 主程序入口
- 事件绑定
- 抽奖流程控制
- 中奖次数管理

**关键方法**：
```javascript
class LotteryApp {
  constructor()
  init()                 // 初始化
  draw()                 // 抽奖
  calculateResult()      // 计算中奖结果
  updateWinCount(prize)  // 更新中奖次数
  resetWinCounts()       // 重置中奖次数
}
```

## 4. 界面设计

### 4.1 配色方案

| 元素 | 颜色 | 用途 |
|------|------|------|
| 主色 | #e74c3c | 按钮、强调 |
| 辅色 | #f39c12 | 转盘扇形 |
| 背景 | #ecf0f1 | 页面背景 |
| 文字 | #2c3e50 | 正文文字 |
| 徽章 | #27ae60 | 中奖次数徽章 |

### 4.2 响应式断点

```css
/* 移动端 */
@media (max-width: 480px) {
  .turntable-canvas { 
    width: 280px; 
    height: 280px; 
  }
  .draw-btn { 
    font-size: 18px; 
    padding: 15px 40px; 
  }
}

/* 平板 */
@media (min-width: 481px) and (max-width: 768px) {
  .turntable-canvas { 
    width: 350px; 
    height: 350px; 
  }
}

/* 桌面 */
@media (min-width: 769px) {
  .turntable-canvas { 
    width: 400px; 
    height: 400px; 
  }
}
```

## 5. 数据流设计

### 5.1 初始化流程

```
页面加载
    │
    ▼
解析 URL 参数 (user, mode)
    │
    ▼
创建 StorageManager（本地存储）
    │
    ▼
加载配置
    │
    └─ 从 localStorage 加载
                         │
                         ├─ 有配置 ──→ 使用保存的配置
                         │
                         └─ 无配置 ──→ 使用默认配置
    │
    ▼
绘制转盘
    │
    ▼
绑定事件
    │
    ▼
等待用户操作
```

### 5.2 抽奖流程

```
点击抽奖按钮
    │
    ▼
检查是否在旋转中
    │
    ├─ 是 ──→ 忽略
    │
    └─ 否 ──→ 继续
    │
    ▼
过滤已达到最大中奖次数的选项
    │
    ▼
计算中奖结果（权重算法）
    │
    ▼
执行转盘动画
    │
    ▼
显示中奖结果
    │
    ▼
更新中奖次数
    │
    ▼
保存配置（异步）
    │
    ├─ 服务端存储 ──→ 发送 API 请求 saveConfig
    │
    └─ 本地存储 ──→ 保存到 localStorage
    │
    ▼
保存抽奖历史（异步）
    │
    ├─ 服务端存储 ──→ 发送 API 请求 saveHistory
    │
    └─ 本地存储 ──→ 保存到 localStorage
    │
    ▼
重置状态
```

### 5.3 配置保存流程

```
打开设置面板
    │
    ▼
加载当前配置到表单
    │
    ▼
用户修改配置
    │
    ▼
点击保存
    │
    ▼
验证配置
    │
    ├─ 有效 ──→ 保存配置（异步）
    │              │
    │              ├─ 服务端存储 ──→ 发送 API 请求 saveConfig
    │              │                     │
    │              │                     ├─ 成功 ──→ 重绘转盘
    │              │                     │
    │              │                     └─ 失败 ──→ 提示网络错误
    │              │
    │              └─ 本地存储 ──→ 保存到 localStorage
    │                                   │
    │                                   ▼
    │                                重绘转盘
    │              │
    │              ▼
    │           更新中奖徽章显示
    │
    └─ 无效 ──→ 显示错误提示
```

## 6. 关键算法

### 6.1 权重抽奖算法（百分比制）

```javascript
function weightedRandom(prizes) {
  // 过滤已达到最大中奖次数的选项
  const availablePrizes = prizes.filter(p => p.winCount < p.maxWins);
  
  if (availablePrizes.length === 0) {
    return null; // 所有选项都已抽完
  }
  
  // 计算总权重
  const totalWeight = availablePrizes.reduce(
    (sum, p) => sum + (p.weight || 10), 0
  );
  
  // 生成随机数
  let random = Math.random() * totalWeight;
  
  // 找到中奖选项
  for (let i = 0; i < availablePrizes.length; i++) {
    random -= availablePrizes[i].weight || 10;
    if (random <= 0) {
      return availablePrizes[i];
    }
  }
  
  return availablePrizes[0];
}
```

### 6.2 转盘角度计算

```javascript
function calculateRotation(prizeIndex, totalPrizes) {
  // 每个选项占的角度
  const anglePerPrize = 360 / totalPrizes;
  
  // 目标角度（从顶部开始）
  const targetAngle = prizeIndex * anglePerPrize + anglePerPrize / 2;
  
  // 多转几圈 + 目标角度
  const totalRotation = 360 * 5 + (360 - targetAngle);
  
  return totalRotation;
}
```

### 6.3 Canvas 绘制转盘

```javascript
function drawTurntable(ctx, prizes, centerX, centerY, radius) {
  const anglePerPrize = (2 * Math.PI) / prizes.length;
  
  prizes.forEach((prize, index) => {
    ctx.save();
    ctx.beginPath();
    ctx.moveTo(centerX, centerY);
    ctx.arc(centerX, centerY, radius, 
            index * anglePerPrize - Math.PI / 2, 
            (index + 1) * anglePerPrize - Math.PI / 2);
    ctx.closePath();
    
    // 填充颜色
    ctx.fillStyle = prize.color || getDefaultColor(index);
    ctx.fill();
    
    // 绘制文字或图标
    ctx.fillStyle = '#fff';
    ctx.font = '14px Arial';
    ctx.textAlign = 'center';
    
    const text = prize.icon || prize.text;
    ctx.fillText(text, 
                 centerX + Math.cos(angle) * radius * 0.7,
                 centerY + Math.sin(angle) * radius * 0.7);
    
    ctx.restore();
  });
}
```

## 7. 服务端 API 设计（可选）

### 7.1 API 接口

**Node.js API** (`src/api/api.js`)：
- 用于 Vercel/Netlify 等平台
- Serverless Function 格式
- 处理配置读写操作
- 支持多设备同步（可选功能）

> **注意**：默认使用 localStorage 本地存储，无需部署 API 即可使用。API 仅在需要多设备同步时才需要部署。

### 7.2 API 操作

| 操作 | 说明 | 请求参数 | 返回数据 |
|------|------|----------|----------|
| saveConfig | 保存配置 | user, mode, config | { success: true } |
| loadConfig | 加载配置 | user, mode | { success: true, config: {...} } |
| clearConfig | 清除配置 | user, mode | { success: true } |
| saveHistory | 保存历史 | user, mode, historyResult | { success: true } |
| loadHistory | 加载历史 | user, mode | { success: true, history: [...] } |
| clearHistory | 清除历史 | user, mode | { success: true } |

### 7.3 用户配置文件结构

```json
{
  "configs": {
    "default": { /* 默认模式配置 */ },
    "team1": { /* team1 模式配置 */ },
    "class1": { /* class1 模式配置 */ }
  },
  "histories": {
    "default": [ /* 默认模式历史 */ ],
    "team1": [ /* team1 模式历史 */ ]
  },
  "createdAt": "2026-04-09T00:00:00.000Z",
  "updatedAt": "2026-04-09T01:00:00.000Z"
}
```

### 7.4 API 安全

- user 参数格式验证（只允许字母、数字、下划线、中划线）
- 配置数据完整性验证
- 错误处理和日志记录
- CORS 配置（允许跨域访问）

## 8. 预制资源

### 7.1 预制图标（使用 Emoji）

```
🎁 礼物     🏆 奖杯     💰 红包     🎫 门票
🎮 游戏     📱 手机     🍰 蛋糕     ✨ 星星
🎈 气球     🌹 玫瑰     💎 钻石     🎯 目标
```

### 7.2 预制颜色

```javascript
const PRESET_COLORS = [
  '#e74c3c', // 红色
  '#e67e22', // 橙色
  '#f1c40f', // 黄色
  '#2ecc71', // 绿色
  '#3498db', // 蓝色
  '#9b59b6', // 紫色
  '#1abc9c', // 青色
  '#34495e'  // 深灰
];
```

## 9. 错误处理

### 9.1 异常情况处理

| 异常情况 | 处理方式 |
|----------|----------|
| localStorage 不可用 | 提示用户并使用内存存储 |
| 配置数据损坏 | 重置为默认配置 |
| Canvas 不支持 | 显示降级提示 |
| 选项列表为空 | 提示添加选项 |
| 所有选项已抽完 | 提示点击重置开始新一轮 |
| 服务端 API 失败 | 提示网络错误，使用本地缓存 |
| user 参数格式错误 | 拒绝请求，返回错误信息 |

### 9.2 错误提示

```javascript
const ERROR_MESSAGES = {
  NO_PRIZES: '请至少添加一个选项',
  STORAGE_ERROR: '本地存储不可用，配置将无法保存',
  CANVAS_ERROR: '您的浏览器不支持 Canvas，请升级浏览器',
  CONFIG_ERROR: '配置数据损坏，已重置为默认配置',
  ALL_DRAWN: '所有选项都已抽完，请点击重置开始新一轮',
  SERVER_ERROR: '配置同步失败，请检查网络连接',
  USER_FORMAT_ERROR: '用户标识格式错误，只允许字母、数字、下划线、中划线'
};
```

## 9. 性能优化

### 9.1 加载优化

- CSS/JS 内联或合并，减少 HTTP 请求
- 图片使用 Emoji 代替，减少资源加载
- 使用 CSS Transform 实现动画，启用 GPU 加速

### 9.2 运行优化

- Canvas 绘制缓存，避免重复绘制
- 事件防抖，避免频繁触发
- 配置变更时才重绘转盘

### 9.3 手机端优化

- 动态计算 Canvas 尺寸，适配不同屏幕
- 添加最小尺寸约束，避免渲染失败
- 改进 DPR 处理，确保清晰度

## 10. 测试计划

### 10.1 功能测试

| 测试项 | 测试内容 | 预期结果 |
|--------|----------|----------|
| 基础抽奖 | 点击按钮抽奖 | 转盘旋转并显示结果 |
| 配置保存 | 修改配置后刷新 | 配置保持不变 |
| 模式切换 | 不同 mode 参数 | 加载对应配置 |
| 权重计算 | 设置不同权重 | 概率分布符合预期 |
| 中奖次数 | 达到最大次数 | 不再参与抽奖 |
| 重置功能 | 点击重置按钮 | 中奖次数清零 |
| 批量导入 | 粘贴多行名称 | 正确导入所有选项 |
| 中奖徽章 | 抽中后显示徽章 | 显示次数正确 |
| 本地存储 | 无 user 参数 | 使用 localStorage |
| 服务端存储 | 有 user 参数 | 使用 API 存储 |
| 多设备同步 | 不同设备相同 user | 配置一致 |
| 多模式配置 | 同一用户多个 mode | 独立配置 |
| API 失败处理 | 网络错误 | 提示错误，使用本地缓存 |

### 10.2 兼容性测试

| 浏览器 | 版本 | 测试结果 |
|--------|------|----------|
| Chrome | 最新 | ✅ 通过 |
| Firefox | 最新 | ✅ 通过 |
| Safari | 最新 | ✅ 通过 |
| Edge | 最新 | ✅ 通过 |
| 移动端 Chrome | 最新 | ✅ 通过 |
| 移动端 Safari | 最新 | ✅ 通过 |

---

**文档版本**: v1.0  
**创建日期**: 2026-04-08  
**更新日期**: 2026-04-09  
**创建人**: 柳飘飘